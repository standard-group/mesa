driver = "sqlite" # change to postgres if you want to use PostgreSQL
sqlite_path = "data/mesa.db"
jwt_secret_key = "super-secret-key"

server_domain = "localhost:8080"

# uncomment and configure if using PostgreSQL
postgres_dsn = "postgres://mesa_user:password@localhost:5432/mesa?sslmode=disable"
